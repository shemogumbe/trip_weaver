version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: dist   # CRA: build ; Next export: out ; SSR: remove artifacts
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
